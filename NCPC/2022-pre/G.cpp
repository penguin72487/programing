#include<bits/stdc++.h>
using namespace std;
int main(){
    cin.tie(0)->sync_with_stdio(0);
    cout.tie(0);
    int t;
    cin >> t;
    while(t--){
        int n,k,q;
        cin >> n>> k >> q;
        vector<int> v(n);
        vector<int> ans(n,0);
        for(auto &x : v) cin >> x;
        for (int i =
    }
    return 0;
}