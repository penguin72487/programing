#include<iostream>
using namespace std;
int main(){
    cin.tie(0)->sync_with_stdio(0);
    cout.tie(0);
    int a, b;
    while(cin>>a>>b)
    {
        cout << 2 * a * b << "\n";
    }
    return 0;
}