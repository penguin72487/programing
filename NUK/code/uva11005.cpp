#include<bits/stdc++.h>
using namespace std;
int main(){
    cin.tie(0)->sync_with_stdio(0);
    cout.tie(0);
    int t;
    cin>>t;
    while(t--)
    {
        int cost[36];
        for(int i=0;i<36;++i)
        {
            cin>>cost[i];
        }
        int n;
        cin>>n;
        cout<<"Case "<<t+1<<":\n";
        for (int i = 0; i < n;++i)
        {
            
        }
    }
    return 0;
}