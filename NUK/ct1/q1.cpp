#include<iostream>
using namespace std;
int main(){
    cin.tie(0)->sync_with_stdio(0);
    cout.tie(0);
    int v, g;
    while(cin>>v>>g)
    {
        cout << 2 * v * g<<"\n";
    }


    return 0;
}