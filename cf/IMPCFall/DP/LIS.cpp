#include<iostream>
#include<vector>
using namespace std;
int main(){
    cin.tie(0)->sync_with_stdio(0);
    cout.tie(0);
    //int n;
    //cin >> n;
    vector<int> a= {10, 9, 2, 5, 3, 7, 101, 4};
    int n = a.size();
    vector<int> dp(a.size() + 1);
    vector<int> v;
    for(int )

    return 0;
}