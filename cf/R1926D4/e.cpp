#include<bits/stdc++.h>
#define endl "\n"
using namespace std;

int main() {


    cin.tie(0)->sync_with_stdio(0);
    cout.tie(0);
    int n = 100;
    map<int, vector<int>> mvi;
    list<int> l;
    for(int i=0;i<n;i++)
    {
        l.push_back(i);
    }
    int di = 1;
    while(!l.empty())
    {
        for(auto)
    }
    for(auto it:mvi)
    {
        cout<<it.first<<": ";
        for(auto i:it.second)
        {
            cout<<i<<" ";
        }
        cout<<endl;
    }
    

    return 0;
}