#include<bits/stdc++.h>

using namespace std;
class Solution {
public:
    int stoneGameII(vector<int>& piles) {
        vector<int> dp(piles.size(), -1);
        
    }
};
int main(){
    cin.tie(0)->sync_with_stdio(0);
    cout.tie(0);
    
    return 0;
}